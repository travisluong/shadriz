import {
  {{#if stripeEnabled}}
  BackpackIcon,
  {{/if}}
  DashboardIcon,
  ExitIcon,
  GearIcon,
  PersonIcon,
  TableIcon,
} from "@radix-ui/react-icons";
import Link from "next/link";

const SIDEBAR_LINKS = [
  { href: "/dashboard", display: "Dashboard", icon: <DashboardIcon /> },
  { href: "/profile", display: "Profile", icon: <PersonIcon /> },
  { href: "/settings", display: "Settings", icon: <GearIcon /> },
  {{#if stripeEnabled}}
  { href: "/account", display: "Account", icon: <BackpackIcon /> },
  {{/if}}
  { href: "/signout", display: "Sign Out", icon: <ExitIcon /> },
];

export default function DashboardSidebar() {
  return (
    <aside className="flex flex-col gap-2 flex-shrink-0 p-5 border-r min-h-screen">
      {SIDEBAR_LINKS.map((link) => (
        <Link
          key={link.href}
          href={link.href}
          className="flex gap-2 items-center"
        >
          {link.icon} {link.display}
        </Link>
      ))}
    </aside>
  );
}
