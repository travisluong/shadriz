import { Table2Icon, LayoutDashboardIcon } from "lucide-react";

import Link from "next/link";

const items = [
  { title: "Dashboard", "url": "/dashboard", icon: LayoutDashboardIcon },
// [CODE_MARK private-sidebar-items]
];

export function PrivateSidebar() {
  return (
    <div className="flex flex-col text-sm min-w-40 max-w-40 bg-zinc-100 dark:bg-zinc-900 fixed min-h-screen z-10 top-8 border-r">
      {items.map((item) => (
        <Link
          key={item.title}
          href={item.url}
          className="flex items-center gap-1 p-1 hover:bg-zinc-200 dark:hover:bg-zinc-800"
        >
          <item.icon className="flex-shrink-0 w-4 h-4" />
          <span>{item.title}</span>
        </Link>
      ))}
    </div>
  );
}
