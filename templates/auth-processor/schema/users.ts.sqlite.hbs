import { sql } from "drizzle-orm";
import { 
  sqliteTable, 
{{dataTypeImports}}
} from "drizzle-orm/sqlite-core"
{{pkStrategyImport}}

export const {{userObj.pluralCamelCase}} = sqliteTable("{{userObj.pluralSnakeCase}}", {
  {{pkText}}
  name: text(),
  email: text().notNull(),
  emailVerified: integer({ mode: "timestamp_ms" }),
  image: text(),
  role: text().notNull(),
  password: text(),
  {{createdAtTemplate}}
  {{updatedAtTemplate}}
  // [CODE_MARK users-table]
})

export type {{userObj.singularPascalCase}} = typeof {{userObj.pluralCamelCase}}.$inferSelect;
