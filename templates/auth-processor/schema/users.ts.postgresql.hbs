import {
  pgTable,
{{dataTypeImports}}
} from "drizzle-orm/pg-core"
{{pkStrategyImport}}

export const {{userObj.pluralCamelCase}} = pgTable("{{userObj.pluralSnakeCase}}", {
  {{pkText}}
  name: text(),
  email: text().notNull(),
  emailVerified: timestamp({ mode: "date" }),
  image: text(),
  role: text().notNull(),
  password: text(),
  {{createdAtTemplate}}
  {{updatedAtTemplate}}
  // [CODE_MARK users-table]
})
 
export type {{userObj.singularPascalCase}} = typeof {{userObj.pluralCamelCase}}.$inferSelect;
