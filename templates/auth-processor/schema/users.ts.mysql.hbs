import {
  mysqlTable,
{{dataTypeImports}}
} from "drizzle-orm/mysql-core"
{{pkStrategyImport}}

export const {{userObj.pluralCamelCase}} = mysqlTable("{{userObj.pluralSnakeCase}}", {
  {{pkText}}
  name: varchar({ length: 255 }),
  email: varchar({ length: 255 }).notNull(),
  emailVerified: timestamp({
    mode: "date",
    fsp: 3,
  }),
  image: varchar({ length: 255 }),
  role: varchar({ length: 255 }).notNull(),
  password: varchar({ length: 255 }),
  {{createdAtTemplate}}
  {{updatedAtTemplate}}
  // [CODE_MARK users-table]
})

export type {{userObj.singularPascalCase}} = typeof {{userObj.pluralCamelCase}}.$inferSelect;
