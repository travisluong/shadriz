export default function Page() {
  return (
    <div className="flex h-screen w-screen justify-center items-center">
      <div className="max-w-md">
        🤔 Looks like you didn't choose an auth provider when you ran `npx
        shadriz init`. Few suggestions: Delete this page. Roll your own auth. Or
        run `npx shadriz init` again.
      </div>
    </div>
  );
}
