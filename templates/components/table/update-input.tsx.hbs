        <div>
          <Label>{{column}}</Label>
          <Input name="{{column}}" defaultValue={ data.{{column}} } />
          {state.errors?.{{column}}?.map((error) => (
            <p className="text-red-500">{error}</p>
          ))}
        </div>