"use client";

import { create{{capitalizedTable}}, Create{{capitalizedTable}}State } from "@/actions/{{table}}/create-{{table}}";
import { Button } from "../ui/button";
import { Input } from "../ui/input";
import { Label } from "../ui/label";
import { Textarea } from "../ui/textarea";
import { Checkbox } from "../ui/checkbox";
import { useFormState } from "react-dom";

export default function {{capitalizedTable}}CreateForm() {
  const initialState: Create{{capitalizedTable}}State = {};
  const [state, dispatch] = useFormState(create{{capitalizedTable}}, initialState);

  return (
    <div>
      <form action={dispatch}>
{{formControls}}
        <Button type="submit">Submit</Button>
        <div>
          {state.message === "error" && <p className="text-red-500">error</p>}
        </div>
      </form>
    </div>
  );
}
