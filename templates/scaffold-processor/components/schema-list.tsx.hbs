// auto-generated by shadriz

import { Card, CardContent, CardHeader, CardTitle } from "./ui/card";

export default function SchemaList() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Schemas</CardTitle>
      </CardHeader>
      <CardContent>
        <ul>
          {{#each tablenames}}
          <li>
            <a className="text-cyan-600" href="/{{this}}">
              {{this}}
            </a>
          </li>
          {{/each}}
        </ul>
      </CardContent>
    </Card>
  );
}
