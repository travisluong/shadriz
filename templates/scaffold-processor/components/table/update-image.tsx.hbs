        <div>
          <Label>{{columnCases.originalCapitalCase}}</Label>
          <Image
            src={ {{tableObj.singularCamelCase}}.{{columnCases.originalCamelCase}} ?? ""}
            width="200"
            height="200"
            alt="{{columnCases.originalCamelCase}}"
          />
          <div>
            <Link href={ {{tableObj.singularCamelCase}}.{{columnCases.originalCamelCase}} ?? ""}>{ {{tableObj.singularCamelCase}}.{{columnCases.originalCamelCase}} ?? "" }</Link>
          </div>
          <Input type="file" name="{{columnCases.originalCamelCase}}" />
          {state.errors?.{{columnCases.originalCamelCase}}?.map((error) => (
            <p className="text-red-500" key={error}>{error}</p>
          ))}
        </div>