        <div className="flex flex-col gap-2">
          <Label>{{validatedColumn.caseVariantsWithId.singularCapitalCase}}</Label>
          <Select name="{{validatedColumn.caseVariantsWithId.singularCamelCase}}">
            <SelectTrigger>
              <SelectValue />
            </SelectTrigger>
            <SelectContent>
              { {{validatedColumn.caseVariants.singularCamelCase}}List.map(({{validatedColumn.caseVariants.singularCamelCase}}) => (
                <SelectItem key={ {{validatedColumn.caseVariants.singularCamelCase}}.id } value={ {{validatedColumn.caseVariants.singularCamelCase}}.id }>
                  { {{validatedColumn.caseVariants.singularCamelCase}}.id}
                </SelectItem>
              ))}
            </SelectContent>
          </Select>
        </div>