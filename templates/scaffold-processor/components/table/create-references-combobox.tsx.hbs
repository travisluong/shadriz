        <div className="flex flex-col gap-2">
          <Label>{{validatedColumn.caseVariantsWithId.singularCapitalCase}}</Label>
          <GenericCombobox
            list={ {{validatedColumn.caseVariants.singularCamelCase}}List }
            name="{{validatedColumn.caseVariantsWithId.singularCamelCase}}"
            valueField="id"
            labelField="id"
            searchPlaceholder="Search {{validatedColumn.caseVariants.pluralCapitalCase}}..."
            selectPlaceholder="Select {{validatedColumn.caseVariants.singularCapitalCase}}..."
            emptyText="No {{validatedColumn.caseVariants.singularCamelCase}} found"
          />
          {state.errors?.{{validatedColumn.caseVariantsWithId.singularCamelCase}}?.map((error) => (
            <p className="text-red-500" key={error}>
              {error}
            </p>
          ))}
        </div>