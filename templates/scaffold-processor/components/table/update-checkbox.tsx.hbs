        <div>
          <Label className="mr-2">{{validatedColumn.caseVariants.originalCapitalCase}}</Label>
          <Checkbox name="{{validatedColumn.caseVariants.originalCamelCase}}" defaultChecked={  {{tableObj.singularCamelCase}}.{{validatedColumn.caseVariants.originalCamelCase}} ?? false } />
          {state.errors?.{{validatedColumn.caseVariants.originalCamelCase}}?.map((error) => (
            <p className="text-destructive" key={error}>{error}</p>
          ))}
        </div>