        <div>
          <Label>{{validatedColumn.caseVariants.originalCapitalCase}}</Label>
          <Input name="{{validatedColumn.caseVariants.singularCamelCase}}" defaultValue={ {{tableObj.singularCamelCase}}.{{validatedColumn.caseVariants.singularCamelCase}} ?? "" } />
          {state.errors?.{{validatedColumn.caseVariants.singularCamelCase}}?.map((error) => (
            <p className="text-destructive" key={error}>{error}</p>
          ))}
        </div>