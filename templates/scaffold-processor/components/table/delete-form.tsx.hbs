"use client";

import { delete{{tableObj.singularPascalCase}}, Delete{{tableObj.singularPascalCase}}State } from "@/actions/{{tableObj.pluralKebabCase}}/delete-{{tableObj.singularKebabCase}}";
import { Button } from "../ui/button";
import { useFormState } from "react-dom";
import { {{tableObj.singularPascalCase}} } from "@/schema/{{tableObj.pluralKebabCase}}";

export default function {{tableObj.singularPascalCase}}DeleteForm({ data }: { data: {{tableObj.singularPascalCase}} }) {
  const initialState: Delete{{tableObj.singularPascalCase}}State = {};
  const [state, dispatch] = useFormState(delete{{tableObj.singularPascalCase}}, initialState);

  return (
    <div>
      <form action={dispatch}>
        <input type="hidden" name="id" value={data.id} />
        <Button type="submit" variant="destructive">
          Delete
        </Button>
      </form>
    </div>
  );
}
