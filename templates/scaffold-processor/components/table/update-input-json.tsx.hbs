        <div>
          <Label>{{validatedColumn.caseVariants.originalCapitalCase}}</Label>
          <Input name="{{validatedColumn.caseVariants.originalCamelCase}}" defaultValue={ JSON.stringify( {{tableObj.singularCamelCase}}.{{validatedColumn.caseVariants.originalCamelCase}}) ?? "" } />
          {state.errors?.{{validatedColumn.caseVariants.originalCamelCase}}?.map((error) => (
            <p className="text-red-500" key={error}>{error}</p>
          ))}
        </div>