import { DashboardIcon, ExitIcon, TableIcon } from "@radix-ui/react-icons";
import Link from "next/link";

const SIDEBAR_LINKS = [
  { href: "/admin", display: "Admin", icon: <DashboardIcon /> },
  { href: "/signout", display: "Sign Out", icon: <ExitIcon /> },
];

export default function AdminSidebar() {
  return (
    <aside className="flex flex-col gap-2 flex-shrink-0 p-5 border-r min-h-screen">
      {SIDEBAR_LINKS.map((link) => (
        <Link
          key={link.href}
          href={link.href}
          className="flex gap-2 items-center"
        >
          {link.icon} {link.display}
        </Link>
      ))}
    </aside>
  );
}
