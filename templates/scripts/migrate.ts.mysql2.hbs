import { migrate } from "drizzle-orm/mysql2/migrator";
import { connection, dbc } from "./dbc";

async function main() {
  await migrate(dbc, { migrationsFolder: "drizzle" });
  await connection.end();
}

main();